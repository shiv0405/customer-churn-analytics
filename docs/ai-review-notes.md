# AI Review Notes

Propose two safe, meaningful maintenance updates: strengthen the churn pipeline with dataset/schema validation and richer evaluation output, and add concise run/testing documentation aligned to the current repository contents.

- `src/churn_pipeline.py` appears truncated in the repository context, so the proposed replacement keeps the existing intent (baseline logistic regression pipeline) while adding safe validation and export behavior.
- The README currently references `data/raw/customer_churn_sample.csv`, but that file is not present in the tree; the documentation update avoids claiming unavailable assets and instead documents the required schema.
- No purely cosmetic churn is proposed; changes focus on safer analytics execution and clearer portfolio usability.
